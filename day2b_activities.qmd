---
title: "eds212_day2_activities"
format: html
---

```{r}
library(palmerpenguins)
library(tidyverse)
library(wesanderson)

```
```{r}
funct <- function(x){
  x^2
}

df_dx <- D(expr = expression(x^2), name = "x")

x = 3

df_dx

```

```{r}
dP_da <- D(expr = expression(2 * (3 * a + 2)^4 - 5), name = "a")

a = 1.5

dP_da
```

```{r}
ggplot(penguins, aes(species, flipper_length_mm, color = species)) +
  geom_jitter() +
  scale_color_manual(values = wes_palette("BottleRocket2")) +
  theme(legend.position = "none") +
  xlab("Penguin Species") +
      ylab("FLipper Length (mm)")
```

